#include <iostream>
#include <fstream>

#include <string>

#include "epch.h"
#include "Terrain.h"

Terrain::Terrain(std::string filepath) {
	float x{ 0 }, y{ 0 }, z{ 0 };

	/*std::ifstream inn;
	inn.open(filepath.c_str());
	if (inn.is_open()) {
		char tea;
		int n;
		inn >> n;
		for (int i = 0; i < n; i++) {
			inn >> tea;
			inn >> tea;
			inn >> tea;
			inn >> tea;
			inn >> tea;
			inn >> tea;


		}
	}*/
}